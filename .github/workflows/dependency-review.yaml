name: Dependency Review - Manual

on:
  workflow_dispatch:
permissions:
  contents: read
  pull-requests: read

jobs:
  dependency-review-note:
    runs-on: ubuntu-latest
    steps:
      - name: Portfolio note
        run: |
          echo "Dependency Review normally runs on pull requests."
          echo "It is manual in this portfolio-ready version to avoid noisy first-upload failures."
